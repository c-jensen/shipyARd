using UnityEngine;
using System.Collections;

public class PlayerReadyScript : MonoBehaviour
{
    //this variable is very important because the player needs to set it to true
    //the information is needed in the tracker for it to work properly
    public bool ready = false;
}
