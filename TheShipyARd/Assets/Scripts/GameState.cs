using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class GameState : MonoBehaviour {
    GameObject playerObject;

    // Use this for initialization
    void Start () {
        //GameObject playerObject = PhotonNetwork.Instantiate("Player", Vector3.zero, Quaternion.identity, 0);
        //playerObject.AddComponent(typeof(PlayerScript));
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
